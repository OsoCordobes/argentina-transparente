# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment — contains API keys / backend URL, never commit
.env
.env.*

# n8n dev/debug scripts
tmp_*.js
tmp_*.cjs

# DuckDB data files — local persistence, not committed
backend/data/
apps/api/data/

# Snapshots — immutable forensic archive, tracked separately not in git
data/snapshots/

# Evidence.dev
reports/.evidence/
reports/build/
reports/node_modules/
reports/argos_snapshot.duckdb

# pnpm
pnpm-lock.yaml

# Package build output (packages/*/dist, apps/*/dist)
packages/*/dist/
apps/api/dist/

# Ralph runs log (can be large)
scripts/ralph/runs/
