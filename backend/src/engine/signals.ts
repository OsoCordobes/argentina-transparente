import type { Contrato, Señal } from '../types'

// Motor de detección de señales de corrupción
export async function detectarSeñales(_contratos: Contrato[]): Promise<Señal[]> {
  // placeholder — implementación próxima sesión
  return []
}
