// Cliente Claude API — placeholder
// Usará @anthropic-ai/sdk en próxima sesión

export async function analizarConClaude(_prompt: string): Promise<string> {
  // placeholder — implementación próxima sesión
  return ''
}
