// Fetcher para datos abiertos de Córdoba Capital
// Fuente: https://gobiernoabierto.cordoba.gob.ar/

export async function fetchContratos(anioDesde: number, anioHasta: number): Promise<Buffer[]> {
  // placeholder — implementación próxima sesión
  // Descargará archivos Excel del portal de datos abiertos
  console.log(`[cordoba-capital] fetchContratos ${anioDesde}–${anioHasta}`)
  return []
}
