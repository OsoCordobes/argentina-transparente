import type { Contrato } from '../../types'

// Parser de archivos Excel de Córdoba Capital
// Usa xlsx para transformar filas en Contrato[]
export function parseContratos(_raw: Buffer[]): Contrato[] {
  // placeholder — implementación próxima sesión
  return []
}
